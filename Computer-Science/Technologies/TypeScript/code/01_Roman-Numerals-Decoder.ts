enum RomanNumericValues {
  I = 1,
  V = 5,
  X = 10,
  L = 50,
  C = 100,
  D = 500,
  M = 1000,
}

export function solution(roman: string): number {
  let result: number;

  Array.from(roman).forEach((i) => {});

  return 0;
}
